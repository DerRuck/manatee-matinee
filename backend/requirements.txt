# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# --- Config ---
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# --- HTTP client (GHL, misc outbound) ---
httpx>=0.27.0

# --- Anthropic / Claude ---
anthropic>=0.34.0

# --- YAML prompt registry (loaded by agents/base.py) ---
pyyaml>=6.0.0

# --- Jinja2 templating (research agent user prompts) ---
jinja2>=3.1.0

# --- Google Cloud ---
google-cloud-firestore>=2.16.0
google-cloud-storage>=2.16.0
google-auth>=2.29.0
google-api-python-client>=2.126.0
google-auth-httplib2>=0.2.0  # AuthorizedHttp wrapper — lets us set a Drive socket timeout
httplib2>=0.22.0  # transport under the Drive client; we construct it with a timeout
google-cloud-aiplatform>=1.49.0  # Vertex text-embedding-005 + future Gemini calls

# --- Document parsers (ingestion pipeline) ---
python-docx>=1.1.0  # parse .docx files from Drive into plain text
pypdf>=4.2.0  # native PDF text extraction (Iflytek transcripts, email attachments)

# --- Document renderers (agent outputs) ---
python-pptx>=1.0.0  # render Presentation Outliner JSON spec into branded decks

# --- Webhook signature verification (Ed25519 for GHL, HMAC in stdlib) ---
# doesnt install locally 
# cryptography>=42.0.0

# --- Dev / test ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0  # httpx mocking
